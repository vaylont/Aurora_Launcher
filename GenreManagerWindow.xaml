<Window x:Class="Aurora_Launcher.GenreManagerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Icon="/ico.ico"
        Title="Управление жанрами"
        SizeToContent="WidthAndHeight"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="#1E1E1E">
    
    <Border Padding="20" Background="#2D2D30" CornerRadius="10">
        <StackPanel>
            <TextBlock Text="Список жанров" 
                       Foreground="White" 
                       FontSize="16" 
                       FontWeight="Bold"
                       Margin="0,0,0,10"/>

            <DataGrid x:Name="GenresGrid"
                      AutoGenerateColumns="False"
                      CanUserAddRows="False"
                      IsReadOnly="True"
                      HeadersVisibility="Column"
                      Width="300" Height="200"
                      Margin="0,0,0,10">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="ID" Binding="{Binding GenreId}" Width="50"/>
                    <DataGridTextColumn Header="Название" Binding="{Binding Name}" Width="*"/>
                </DataGrid.Columns>
            </DataGrid>

            <!-- Вот здесь убрали PlaceholderText -->
            <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                <TextBox x:Name="TbGenreName"
                         Width="200" Height="25"
                         VerticalContentAlignment="Center"
                         Text="" />
                <Button Content="Добавить" Width="70" Margin="10,0,0,0"
                        Click="AddGenre_Click"/>
            </StackPanel>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Переименовать" Width="100" Margin="0,0,10,0"
                        Click="RenameGenre_Click"/>
                <Button Content="Удалить" Width="70" 
                        Click="DeleteGenre_Click"/>
            </StackPanel>
        </StackPanel>
    </Border>
</Window>
